{
  "name": "tenth-avenue-web-design",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend",
    "test": "node scripts/test-functions.mjs",
    "check:html": "node scripts/check-html.mjs",
    "check:bundle": "node scripts/check-bundle.mjs",
    "check": "npm run build --prefix frontend && node --check functions/_shared.js && node --check functions/api/contact.js && node --check functions/api/quote.js && npm run check:html && npm run check:bundle && npm run check:routes && npm test",
    "pages:dev": "npm run build --prefix frontend && wrangler pages dev frontend/dist",
    "pages:deploy": "npm run build --prefix frontend && wrangler pages deploy frontend/dist",
    "check:routes": "node scripts/check-routes.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
